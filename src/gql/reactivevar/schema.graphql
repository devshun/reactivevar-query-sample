type Sample {
  id: Int!
  name: String!
  email: String!
  isActive: Boolean!
  createdAt: String!
  # this field is not defined in reacttivevar
  unknownField: String!
}

extend type Query {
  sample: Sample
}
